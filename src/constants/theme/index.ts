import { Colors } from './colors';
import { Typography } from './typography';

export const Theme = {
  colors: Colors,
  typography: Typography,
  
  spacing: {
    xs: 4,
    sm: 8,
    md: 16,
    lg: 24,
    xl: 32,
    gutter: 16,
  },

  borderRadius: {
    none: 0,
    sm: 4,
    md: 8,
    lg: 16,
  }
};

export default Theme;